{
  "name": "mhm-pipeline-web",
  "version": "0.1.0",
  "description": "Heroku root shim — Node buildpack uses this to build the Vite frontend during slug compile.",
  "private": true,
  "engines": { "node": "20.x" },
  "scripts": {
    "heroku-postbuild": "cd frontend && npm install --include=dev --no-audit --no-fund && npm run build"
  }
}
