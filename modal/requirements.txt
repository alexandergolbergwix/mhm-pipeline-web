torch==2.5.1
transformers==4.46.3
huggingface_hub>=0.26.0,<2.0
safetensors>=0.4.5
numpy<2.0
fastapi>=0.115
# Imported at module-top by desktop training scripts that we
# transitively pull in for the model class definitions
# (JointModel, NERModel). We don't use the training half of those
# scripts at all, but Python evaluates the imports anyway when we
# `from train_joint_entity_role_model_kfold import JointModel` etc.
# Surfaced one-by-one by `modal app logs mhm-ner` after the
# @modal.enter() crash. Add a new line here every time the logs
# show a new ModuleNotFoundError on cold start.
scikit-learn>=1.4
seqeval>=1.2
