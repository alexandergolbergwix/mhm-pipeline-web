# /typecheck

Run the frontend TypeScript compiler in strict no-emit mode.

```bash
cd /Users/alexandergo/Documents/Doctorat/mhm-pipeline-web/frontend
yarn tsc --noEmit
```

Must pass clean before any commit that touches `.tsx` / `.ts`.
